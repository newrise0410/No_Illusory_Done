1. (a) REJECTED as a bypass. With PRODUCT: tests, src, the implementer-added src/app.py made the frozen test pass (G1 PASS / ALL MET). Editing the frozen tests/test_product.py then made --run fail closed before execution: `NID FAIL: refusing: freeze mismatch before run` / `modified tests/test_product.py`. Product scope does not override FILES hashes; ordinary new files under tests remain in scope.
2. (b) CONFIRMED. Frozen tests/test_import.py imports src.util, but src/util.py is PRODUCT and is not in FILES. After freeze, adding util.check() -> True made `--run` pass (G1 PASS / ALL MET) while the actual product logic can remain elsewhere and untested. The imported product helper is an editable oracle boundary.
3. (c) REJECTED. After freeze, adding both src/__init__.py and src/package.json under PRODUCT caused --run to fail before the gate: `runner-influencing files changed since the freeze are not frozen or EXPECTED_NEW: src/__init__.py, src/package.json`. PRODUCT does not exempt influence names.
